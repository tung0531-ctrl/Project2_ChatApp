// Wrapper khai bao bien the clinic goc dung SVM.
import { createClinicBotDefinition } from "../runtime/definitionFactory.js";

export const createBotClinicDefinition = () => createClinicBotDefinition("botClinic");
